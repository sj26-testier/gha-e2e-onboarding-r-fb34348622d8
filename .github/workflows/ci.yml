{
  "name": "Self repository parity",
  "on": {
    "pull_request": {
      "types": [
        "opened",
        "synchronize"
      ]
    }
  },
  "permissions": {
    "contents": "read"
  },
  "jobs": {
    "seed": {
      "runs-on": "ubuntu-latest",
      "timeout-minutes": 5,
      "steps": [
        {
          "run": "echo seed-only"
        }
      ]
    }
  }
}
